<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/CameraLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.vvdev.colorpicker.ui.Camera.AutoFitTextureView
        android:id="@+id/TextureViewCamera"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/BottomBar"
        android:layout_below="@id/TopBar" />

    <include
        android:id="@+id/TopBar"
        layout="@layout/camera_top"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true" />

    <FrameLayout
        android:layout_width="32dp"
        android:layout_height="140dp"
        android:layout_alignBottom="@id/BottomBar"
        android:layout_marginBottom="110dp"
        android:orientation="vertical">

        <SeekBar
            android:id="@+id/SeebarZoom"
            android:layout_width="140dp"
            android:layout_height="32dp"
            android:layout_gravity="center"
            android:rotation="270" />

    </FrameLayout>
    <include
        android:id="@+id/BottomBar"
        layout="@layout/camera_bottom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center|end"
        android:orientation="vertical">

        <include
            layout="@layout/camera_middle_right"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/BottomBar"
        android:layout_below="@id/TopBar"
        android:gravity="center_horizontal|center_vertical"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/CameraCircle"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_gravity="center_horizontal|center_vertical"
            android:background="@drawable/white_rounded_area"
            app:srcCompat="@android:color/transparent" />

    </FrameLayout>


</RelativeLayout>